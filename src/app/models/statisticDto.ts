export interface StatisticDto {
    id: number;                // ID statistike
    points: number;            // Broj poena
    asists: number;           // Broj asistencija
    rebounds: number;
    fouls:number;          // Broj skokova
    // Dodaj ostale atribute statistike ako su prisutni
  }
  